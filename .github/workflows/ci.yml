name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Qt headless runtime
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes libegl1
      - name: Install project and development tools
        run: python -m pip install ".[dev]"
      - name: Ruff lint
        run: python -m ruff check .
      - name: Ruff format
        run: python -m ruff format --check .
      - name: Mypy
        run: python -m mypy src
      - name: Tests
        run: python -m pytest
      - name: Installed CLI smoke checks
        env:
          DBUS_SESSION_BUS_ADDRESS: unix:path=/tmp/lyriflux-ci-session-bus
        run: |
          lyriflux --version
          lyriflux doctor

  release-artifact:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Linux desktop validation runtime
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes desktop-file-utils g++ libegl1 libicu-dev pkg-config
      - name: Install release build tools
        run: python -m pip install ".[dev]"
      - name: Build and compare release artifacts
        run: python scripts/build_release.py --output-dir /tmp/lyriflux-dist
      - name: Clean-environment artifact install and lifecycle gate
        env:
          DBUS_SESSION_BUS_ADDRESS: unix:path=/tmp/lyriflux-ci-session-bus
          XDG_CONFIG_HOME: /tmp/lyriflux-fresh/config
          XDG_CACHE_HOME: /tmp/lyriflux-fresh/cache
          XDG_DATA_HOME: /tmp/lyriflux-fresh/data
        run: |
          python -m venv /tmp/lyriflux-fresh/venv
          /tmp/lyriflux-fresh/venv/bin/python -m pip install /tmp/lyriflux-dist/*.whl
          /tmp/lyriflux-fresh/venv/bin/lyriflux --version
          /tmp/lyriflux-fresh/venv/bin/lyriflux doctor
          /tmp/lyriflux-fresh/venv/bin/lyriflux storage migrate
          /tmp/lyriflux-fresh/venv/bin/lyriflux storage backup /tmp/lyriflux-fresh/backup.sqlite3
          /tmp/lyriflux-fresh/venv/bin/lyriflux diagnostics export --output /tmp/lyriflux-fresh/diagnostics.json
          /tmp/lyriflux-fresh/venv/bin/lyriflux desktop-integration install
          desktop-file-validate /tmp/lyriflux-fresh/data/applications/io.github.myonctl.LyriFlux.desktop
          /tmp/lyriflux-fresh/venv/bin/lyriflux desktop-integration status
          /tmp/lyriflux-fresh/venv/bin/lyriflux desktop-integration remove
          test -f /tmp/lyriflux-fresh/data/lyriflux/lyriflux.sqlite3
          /tmp/lyriflux-fresh/venv/bin/python -m pip uninstall --yes lyriflux
          test -f /tmp/lyriflux-fresh/data/lyriflux/lyriflux.sqlite3

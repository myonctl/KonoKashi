name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Qt headless runtime
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes libegl1
      - name: Install project and development tools
        run: python -m pip install ".[dev]"
      - name: Ruff lint
        run: python -m ruff check .
      - name: Ruff format
        run: python -m ruff format --check .
      - name: Mypy
        run: python -m mypy src
      - name: Tests
        run: python -m pytest
      - name: Installed CLI smoke checks
        env:
          DBUS_SESSION_BUS_ADDRESS: unix:path=/tmp/lyricflow-ci-session-bus
        run: |
          lyricflow --version
          lyricflow doctor

  release-artifact:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Linux desktop validation runtime
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes desktop-file-utils g++ libegl1 libicu-dev pkg-config
      - name: Install release build tools
        run: python -m pip install ".[dev]"
      - name: Build and compare release artifacts
        run: python scripts/build_release.py --output-dir /tmp/lyricflow-dist
      - name: Clean-environment artifact install and lifecycle gate
        env:
          DBUS_SESSION_BUS_ADDRESS: unix:path=/tmp/lyricflow-ci-session-bus
          XDG_CONFIG_HOME: /tmp/lyricflow-fresh/config
          XDG_CACHE_HOME: /tmp/lyricflow-fresh/cache
          XDG_DATA_HOME: /tmp/lyricflow-fresh/data
        run: |
          python -m venv /tmp/lyricflow-fresh/venv
          /tmp/lyricflow-fresh/venv/bin/python -m pip install /tmp/lyricflow-dist/*.whl
          /tmp/lyricflow-fresh/venv/bin/lyricflow --version
          /tmp/lyricflow-fresh/venv/bin/lyricflow doctor
          /tmp/lyricflow-fresh/venv/bin/lyricflow storage migrate
          /tmp/lyricflow-fresh/venv/bin/lyricflow storage backup /tmp/lyricflow-fresh/backup.sqlite3
          /tmp/lyricflow-fresh/venv/bin/lyricflow diagnostics export --output /tmp/lyricflow-fresh/diagnostics.json
          /tmp/lyricflow-fresh/venv/bin/lyricflow desktop-integration install
          desktop-file-validate /tmp/lyricflow-fresh/data/applications/io.github.myonctl.LyricFlow.desktop
          /tmp/lyricflow-fresh/venv/bin/lyricflow desktop-integration status
          /tmp/lyricflow-fresh/venv/bin/lyricflow desktop-integration remove
          test -f /tmp/lyricflow-fresh/data/lyricflow/lyricflow.sqlite3
          /tmp/lyricflow-fresh/venv/bin/python -m pip uninstall --yes lyricflow
          test -f /tmp/lyricflow-fresh/data/lyricflow/lyricflow.sqlite3
